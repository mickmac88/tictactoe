extends ./layout.jade

block title
  title All the users!
  
block content
  h1 All the users!
  ul
    each user in users
      li
        a(href="/users/#{user.id}") User ##{user.id}: #{user.username}
  if errors
    p.text-danger= errors.message
